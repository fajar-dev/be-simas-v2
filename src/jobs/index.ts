/**
 * Jobs - Standalone scripts for scheduled tasks
 * 
 * Available commands:
 * - bun run sync:organization      - Sync organizations from Nusawork
 * - bun run sync:employee          - Sync employees from Nusawork
 * - bun run sync:user              - Sync users from Nusawork
 * - bun run sync:branch            - Sync branches from Nusawork
 * - bun run sync:nusawork          - Sequentially run sync:branch -> sync:organization -> sync:employee -> sync:user
 * - bun run job:asset-schedule-reminder
 * - bun run queue:work
 */

