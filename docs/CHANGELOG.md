# 📝 Changelog

Semua perubahan penting pada proyek ini akan didokumentasikan di file ini.

Format mengikuti [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

---

## [Unreleased] — 2026-07-19

### Changed
- **`id` pada respons statistik per-kategori/lokasi/sub-kategori**: `GET /statistic/assets-by-category`, `/assets-by-location`, `/assets-by-sub-category` kini menyertakan `id` di tiap item (sebelumnya hanya `name`/`count`/`totalPrice`/`totalBookValue`) — dipakai FE agar legenda chart di Dashboard bisa diklik langsung ke `GET /asset?categoryIds=`/`locationIds=`/`subCategoryIds=` tanpa perlu mencocokkan nama. Test & swagger diperbarui.

### Fixed
- **PDF handover: "Nama Karyawan" salah untuk Pengembalian**: sebelumnya kolom ini selalu mengambil nama penerima (`receivedBy`), padahal untuk Pengembalian yang relevan adalah karyawan yang menyerahkan kembali (`handedOverBy`) — sekarang mengikuti tipe transaksi: **Penetapan** → nama penerima, **Pengembalian** → nama yang menyerahkan. Logikanya diekstrak ke `resolveEmployeeName()` (di-export dari `handover-pdf.ts`) agar bisa diuji langsung tanpa parsing byte PDF. Test baru di `test/handover-pdf.test.ts`.
- **`POST /handover` gagal (500) bila nama karyawan/aset/item atau catatan mengandung karakter kontrol**: `generateHandoverPdf` memakai font standar (WinAnsi encoding) yang melempar error saat menggambar teks berisi byte kontrol (mis. `0x02` — biasa muncul dari data sync/import yang kotor), menggagalkan seluruh pembuatan handover (bukan cuma PDF-nya) karena generate PDF terjadi di tengah alur `create`. Ditambah helper `sanitizeText()` di `handover-pdf.ts` yang membuang karakter kontrol sebelum teks digambar — diterapkan di `wrapText` (menutupi seluruh sel tabel item & info) serta pada label baris dan nama tanda tangan yang sebelumnya digambar langsung tanpa lewat `wrapText`. Test regresi baru `test/handover-pdf.test.ts` (unit test murni, tanpa DB) yang mereproduksi crash persis seperti laporan lalu memverifikasi PDF berhasil dibuat setelah fix.
- **`create`/`update` Employee & Organization tidak menyertakan objek relasi yang baru di-set**: `EmployeeService`/`OrganizationService` sebelumnya mengembalikan entity langsung dari `repository.save()`, yang tidak memuat relasi (`organization`/`parent`) sama sekali — field relasi selalu `null` di respons meski `organizationId`/`parentId` valid. Lebih parah lagi, pada **update** yang mengubah relasi yang *sudah* ter-set sebelumnya (mis. pindah organisasi/parent lama ke yang baru), objek relasi basi yang ter-load otomatis oleh `getById()` di awal `update()` malah **menimpa balik** scalar FK yang baru saat `save()` (TypeORM memprioritaskan objek relasi yang sudah dimuat di atas kolom scalar yang baru di-merge). Kedua service kini eksplisit meng-clear properti relasi sebelum `merge()` (persis pola yang sudah dipakai `UserService` untuk `role`/`employee`), dan me-reload via `getById()` setelah `save()` agar respons `create`/`update` selalu menyertakan relasi terbaru. Test regresi ditambahkan di `test/employee.test.ts` & `test/organization.test.ts`.

### Added
- **Sinkronisasi hapus ke Nusawork saat asset holder dihapus**: melengkapi trigger edit di atas — `DELETE /asset-holder/:id` sekarang juga menghapus catatan Nusawork-nya (`DELETE .../note/web/202/employee?employee_id=...&id_group=...`) lewat `NusaworkHelper.deleteAssetSync()` baru, dipanggil setelah record lokalnya benar-benar terhapus. `id_group`-nya dicari dengan mekanisme yang sama (`AssetHolderService.findNusaworkGroupId()`, dipakai bersama oleh return/update/delete sync). Organization holder tidak pernah memicu ini; best-effort seperti sinkronisasi lain (kegagalan Nusawork tidak pernah membatalkan penghapusan). Test baru di `test/asset-holder.test.ts` (payload delete yang benar, tetap sukses saat sinkronisasi gagal, tidak terpicu untuk organization).
- **Sinkronisasi ulang ke Nusawork saat data assign/return/aset diedit**: melengkapi sinkronisasi assign & return di atas — sekarang catatan Nusawork juga ikut diperbarui saat data yang sudah tersinkron itu sendiri diedit, bukan cuma saat pertama kali dibuat. Dua trigger: (1) **`PUT /asset-holder/:id`** — tiap kali record assign/return-nya diedit (tanggal, catatan, dll) dan hasilnya masih employee holder, seluruh field (`asset_code`, `asset_name`, `assign_date`, `assign_note`, `return_date`, `return_note`) dikirim ulang ke Nusawork sesuai data terbaru di DB kita; (2) **`PUT /asset/:id`** — bila `code` atau `name` aset berubah, **semua** record `AssetHolder` employee milik aset itu (aktif maupun riwayat) ikut disinkronkan ulang, supaya catatan lama di Nusawork tidak menampilkan kode/nama aset yang sudah usang. Berbeda dari create (POST, buat catatan baru) dan return (PUT, hanya kirim field return), trigger ini memakai method PUT baru `NusaworkHelper.updateAssetSync()` yang mengirim seluruh field sekaligus (endpoint tidak melakukan partial-merge yang aman diasumsikan), dengan `id_group` dicari lewat mekanisme yang sama (retry + `id_holder` matching) yang dipakai sinkronisasi return — logic pencarian `id_group` diekstrak jadi satu method bersama `AssetHolderService.findNusaworkGroupId()` supaya tidak duplikat antara return & update sync. Organization holder tidak pernah memicu sinkronisasi ini. Repository baru `AssetHolderRepository.findEmployeeHeldByAssetId()` untuk mengambil seluruh riwayat holder employee suatu aset. Test baru di `test/asset-holder.test.ts` (edit assign/return data men-trigger update dengan payload lengkap, switch ke organization tidak trigger) dan `test/asset.test.ts` (ubah code/name men-sinkronkan ulang holder aktif+riwayat dengan `id_group` masing-masing, field lain seperti price tidak men-trigger apa-apa).
- **Sinkronisasi return-dari-employee ke Nusawork**: melengkapi sinkronisasi assign di atas — setiap kali aset **dikembalikan** oleh employee (bukan organization), dari jalur manapun: return langsung (`POST /asset-holder/:id/return`), return lewat serah terima yang disetujui (`HandoverService.applyReturn`), pengembalian buku (`BookService.returnBook`), dan auto-return saat status aset diubah (`AssetStatusService.returnActiveHolderForAsset`, keduanya otomatis ikut tercakup karena sama-sama lewat `AssetHolderService.returnAsset()` tanpa perubahan tambahan). Berbeda dari assign, endpoint return Nusawork (`PUT .../note/web/202/employee`) butuh `id_group` — bukan id yang kita simpan sendiri, jadi dicari dulu lewat `NusaworkHelper.getAssetSyncGroups()` (`GET .../note/web/202/employee?employee_id=...` — note-type id yang sama dengan assign, bukan id terpisah) memindai grup `data[]` untuk item dengan `key: "id_holder"` yang `input.value`-nya cocok dengan id record `AssetHolder` ini, baru `id_group` grup tsb dipakai untuk `NusaworkHelper.returnAssetSync()`. Karena catatan yang dibuat saat assign butuh waktu untuk muncul di sisi baca Nusawork, pencarian grup ini di-retry sampai 3x (jeda 1 detik) sebelum menyerah; respons yang bentuknya tak terduga (mis. auth/permission gagal) di-log lengkap dengan status HTTP & body-nya lewat `getAssetSyncGroups()` sendiri agar mudah didiagnosis. Bila tetap tidak ada grup yang cocok setelah retry (mis. assign-nya terjadi sebelum fitur sinkronisasi ini ada, atau sinkronisasi assign-nya sendiri gagal), return tetap sukses dan sinkronisasi dilewati (di-log lengkap dengan daftar `id_holder` yang ditemukan untuk memudahkan investigasi, tidak pernah menggagalkan return yang sebenarnya) — sama best-effort-nya dengan sisi assign. Logic dipusatkan di `AssetHolderService.notifyNusaworkReturn()`, dipanggil setelah transaksi DB commit. Payload `fields`: `return_date` (format datetime `"YYYY-MM-DD HH:mm:ss"` yang sama dengan `assign_date`) dan `return_note` (opsional). Test baru di `test/asset-holder.test.ts` (payload & `id_group` yang benar, tidak terpicu untuk organization, tetap sukses saat grup tidak ditemukan maupun saat sinkronisasi gagal) dan `test/handover.test.ts` (return via handover); mock Nusawork di semua test file terkait ditambah method `getAssetSyncGroups`/`returnAssetSync`.
- **Sinkronisasi assign-ke-employee ke Nusawork**: `NusaworkHelper.createAssetSync()` (sudah ada sebelumnya, tapi tidak pernah dipanggil) sekarang otomatis dipanggil setiap kali sebuah aset ditetapkan ke **employee** (bukan organization), dari jalur manapun: assign langsung (`POST /asset-holder`), holder awal saat membuat aset (`POST /asset` dengan `employeeId`), assign lewat serah terima yang disetujui (`HandoverService.applyAssign`), dan pinjam buku (`BookService.borrow`, yang secara internal sudah lewat `AssetHolderService.create()` sehingga otomatis ikut tercakup tanpa perubahan tambahan). Logic dipusatkan di satu method baru `AssetHolderService.notifyNusaworkAssignment()` — dipanggil **setelah** transaksi DB commit (bukan di dalamnya, supaya panggilan HTTP ke pihak ketiga tidak menahan koneksi/transaksi DB), dan **best-effort**: kegagalan Nusawork (timeout, error, dsb.) hanya di-log ke console, tidak pernah menggagalkan assignment yang sebenarnya. Assign ke organization tidak pernah memicu sinkronisasi ini (Nusawork hanya mengenal employee). Payload `fields` yang dikirim: `asset_code`, `asset_name`, `assign_date` (selalu diformat penuh sebagai datetime `"YYYY-MM-DD HH:mm:ss"`, mis. `"2026-06-19 00:00:00"`, lewat `formatNusaworkDateTime()` — `assignedDate` di database sendiri tetap string bebas format/varchar tanpa constraint tanggal, konversi ini murni untuk memenuhi format API Nusawork), `assign_note` (opsional), dan `id_holder` (id record `AssetHolder` yang baru dibuat, dikirim di setiap jalur assign). Test baru/diperbarui di `test/asset-holder.test.ts`, `test/asset.test.ts`, `test/handover.test.ts` (memverifikasi payload yang dikirim, tidak terpicu untuk organization, dan assignment tetap sukses walau sinkronisasi gagal); mock Nusawork ditambahkan ke semua test file lain yang menyentuh jalur assign-ke-employee agar tidak melakukan panggilan jaringan sungguhan (`test/book.test.ts`, `test/handover-stock.test.ts`, `test/asset-status.test.ts`, `test/asset-log.test.ts`, `test/employee.test.ts`).
- **Kolom Age pada export Excel Asset**: `GET /asset/export` menambah kolom **Age** (umur aset sejak `purchaseDate`, format "X years Y months Z days") tepat setelah Purchase Date — dihitung di `AssetUtilService.calculateAge()`, kosong bila aset tidak punya `purchaseDate`.
- **Modul Stock Opname pada Persediaan**: modul baru `inventory-stock-opname` (tabel `inventory_stock_opname` + `inventory_stock_opname_items`), disusun persis seperti `inventory-stock-in` (dokumen header + line item, bukan alur pending/approve seperti handover). Beda dengan `POST /inventory-stock-in` yang bersifat **ADD** (menambah on-hand) dan `POST /inventory/stock/entry` yang lama (set absolut tapi **tanpa jejak dokumen** — sudah ada test regresi yang memastikan itu), `POST /inventory-stock-opname` bersifat **SET** (hasil hitung fisik) **dan** menyimpan riwayatnya: tiap baris menyimpan `systemQuantity` (saldo tercatat sebelum dihitung), `countedQuantity` (hasil hitung fisik), dan `quantity` (selisih bertanda = counted − system) — baris hanya dibuat bila hasil hitung berbeda dari saldo tercatat (varian/kondisi yang cocok dilewati, sama seperti `stock-in` melewati baris dengan qty 0). Satu dokumen opname di-scope ke **satu cabang** (branch di level dokumen, bukan per-item — beda dari stock-in yang boleh multi-cabang per baris) karena hitung fisik dilakukan di satu lokasi. Method baru `InventoryStockService.setBalance()` (di-share, dipakai satu-satunya oleh modul ini) melakukan locked-read lalu set nilai absolut sekaligus mengembalikan `previousQuantity`/`delta` untuk dicatat di baris item. `POST /inventory-stock-opname` (permission baru `inventory-stock:opname`, di-seed) & `GET /inventory-stock-opname?inventoryId=` (permission `inventory-stock:read`, dipakai bersama). Lampiran & log aktivitas (`module: "stock", action: "opname"`) mengikuti pola yang sama seperti stock-in. Test baru `test/inventory-stock-opname.test.ts` (13 test: set bukan tambah, baris yang sama dilewati, delta negatif, cabang tanpa saldo sebelumnya, tidak memengaruhi cabang lain, tolak varian di luar item, tolak cabang tak ada, bentuk respons dokumen+item+cabang, riwayat paginasi, `entry` lama tetap tidak membuat dokumen opname, `inventoryId` wajib, 404 item tak ada). Swagger diperbarui.
- **`PUT`/`DELETE /asset-holder/:id`**: sebelumnya satu-satunya cara mengubah catatan asset holder adalah lewat `POST .../return` (hanya untuk pengembalian) — sekarang catatan assign/return juga bisa diedit langsung, dan bisa dihapus. Field mana yang boleh diedit tergantung status record: selama masih aktif (belum dikembalikan), hanya field sisi assign (`holderKind`, `employeeId`/`organizationId`, `assignedDate`, `assignNote`, `attachmentIds`) yang bisa diubah — mengirim `returnedDate`/`returnNote` ditolak (400) karena belum ada pengembalian untuk diedit; setelah record dikembalikan, field sisi return ikut terbuka untuk diedit. Validasi holder (karyawan/organisasi harus ada & aktif) dan exclusivity `employeeId`/`organizationId` dijalankan ulang persis seperti saat assign. `attachmentIds`, bila dikirim, mengganti seluruh set lampiran (pola `disassociateOrphans` + `associate`, sama seperti `AssetMaintenanceService.update`). Delete menghapus record beserta lampirannya — bila record tsb adalah active holder aset, aset otomatis jadi tak berpemegang. Permission baru `asset-holder:update`/`asset-holder:delete` (di-seed). Test baru di `test/asset-holder.test.ts` (10 test: edit assign-only saat aktif, tukar employee↔organization, tolak edit return saat aktif, tolak holder tidak aktif, tolak exclusivity, edit assign+return setelah dikembalikan, 404, delete sukses + verifikasi terhapus, delete holder aktif membebaskan aset, delete 404). Swagger diperbarui.
- **Active Holder aset kini bisa juga Organization, tidak hanya Employee**: sebelumnya `AssetHolder.employeeId` wajib diisi, padahal beberapa aset (peralatan bersama/komunal) dipakai satu departemen, bukan satu orang. Ditambah diskriminator `holderKind` (`employee`/`organization`, enum baru `ASSET_HOLDER_KINDS` di `core/enums.ts`) plus `organizationId` nullable berdampingan dengan `employeeId` (kini juga nullable) — persis pola `InventoryStockOut.isEmployee`, exclusivity (XOR) divalidasi di Zod `superRefine` + service layer, **bukan** DB constraint. `POST /asset-holder` & `POST /asset` (assign holder awal saat membuat aset) menerima `holderKind`+`organizationId`; `holderKind` default `"employee"` bila tidak dikirim agar payload FE lama (tanpa field ini) tetap jalan. `GET /asset` mendapat filter `holderKind` baru (default `employee`, dipakai bareng `holderId` yang sudah ada) — ikut berlaku otomatis di export Excel karena berbagi `parseFilters()` yang sama; kolom pencarian & sort holder (`COALESCE(activeEmployee.name, activeOrganization.name)`) ikut menyertakan nama organisasi. Export Excel (`GET /asset/export`) dapat kolom baru **Holder Type** (Employee/Organization) di depan Holder Name; Holder Employee ID otomatis kosong untuk holder organisasi. `DELETE /organization/:id` diblokir (409) bila organisasi tsb masih jadi active holder aset — pola sama seperti guard `countChildren`. Handover & Book (`applyAssign`/`borrow`) tetap **employee-only** tanpa perubahan perilaku, hanya disesuaikan agar tetap mengirim `holderKind: "employee"` ke skema baru — return-handover terhadap aset yang dipegang organisasi otomatis tertolak lewat perbandingan `employeeId` yang sudah nullable (tanpa logic tambahan). Test baru di `test/asset-holder.test.ts` (7 test varian organization), `test/asset.test.ts` (filter `holderKind` + create dengan `organizationId`), `test/organization.test.ts` (delete diblokir), `test/handover.test.ts` (regresi return-handover ditolak untuk aset organization-held). Swagger diperbarui.
- **Modul Organization (struktur organisasi parent-child)**: modul baru `organization` (tabel `organizations`, self-referencing lewat `parent_id`) — tiap organisasi punya `name`, `type` (teks bebas, mis. `division`/`department` mengikuti Nusawork), `description`, `isActive`, dan opsional `parentId` untuk kedalaman tak terbatas. Endpoint CRUD `GET/POST /organization` (yang `GET`-nya **paginated** — page/limit/q/sortBy/order, `sortBy=parent` ikut nama induk — sama seperti pola `GET /category`; tiap baris menyertakan `parent: { id, name }`), `GET /organization/list` (datar tanpa paginasi, untuk dropdown/parent-picker, tanpa permission khusus seperti pola `/category/list`), `GET/PUT/DELETE /organization/:id` (permission `organization:read|create|update|delete`, di-seed). Update parent divalidasi anti-siklus (tidak bisa jadi parent diri sendiri atau pindah ke bawah keturunannya sendiri) via `OrganizationService.getDescendantIds()`. Delete diblokir (409) bila masih punya sub-organisasi, sama seperti pola Category/SubCategory. Flatten data eksternal Nusawork ada di `organization-tree.ts` (`flattenNusaworkOrganizations`, pure function, unit-testable tanpa DB).
- **`bun run sync:organization`**: job standalone baru (`src/jobs/sync-organization.job.ts`, mengikuti pola persis `sync-employee`/`sync-branch` — tanpa endpoint HTTP, dijadwalkan lewat cron eksternal) yang meng-crawl `GET /emp/api/organization` dari Nusawork (`nusaworkHelper.getOrganization()` baru) dan meng-upsert by id. API Nusawork mengembalikan tiap node dua kali (top-level & bersarang di `childs` leluhurnya) — `flattenNusaworkOrganizations()` men-dedupe by id sebelum upsert. Upsert dilakukan **dua pass** (pass 1 tanpa `parentId`, pass 2 baru set `parentId` sebenarnya) supaya FK self-referencing tidak pernah menunjuk baris yang belum ada, terlepas urutan parent/child dari API. Test `test/organization.test.ts` (22 test, termasuk unit test pure function tree/flatten + full CRUD + cycle-guard + delete-blocked-with-children) & swagger diperbarui.
- **`organizationId` pada Employee, diisi otomatis lewat sync**: kolom baru `organization_id` (FK nullable ke `organizations`, `onDelete: SET NULL`) — `POST/PUT /employee` juga menerima `organizationId` untuk override manual, dan `EmployeeResponse` menyertakan `organization: { id, name }`. `sync:employee` sekarang mencocokkan `organization_name` dari tiap baris Nusawork terhadap `Organization.name` (case-insensitive, ter-trim) untuk mengisi `organizationId` — jika tidak ada organisasi yang cocok (mis. `sync:organization` belum pernah dijalankan), dibiarkan `null` dan dicatat di log ringkasan. Bisa dipakai sebagai `sortBy` (ikut nama organisasi). Test `test/employee.test.ts` diperbarui (termasuk kasus reassign ke organisasi lain).
- **`GET /asset/options`**: endpoint pencarian aset yang ringan untuk picker/select (`q` cocok ke nama/kode, `limit` default 20 maks 50) — hanya mengembalikan `id`/`code`/`name`/`image`, tanpa join relasi (holder/lokasi/kategori/label seperti `GET /asset` biasa). Dipakai FE (kalender: filter aset & modal Tambah Jadwal) sebagai pengganti memuat halaman pertama daftar aset penuh — jauh lebih murah pada tabel aset yang bisa berisi ratusan ribu baris. Repository baru `AssetRepository.searchOptions()` (query 1 tabel, tanpa join). Test & swagger diperbarui.
- **`GET /handover/pending-assets`**: endpoint baru yang mengembalikan `assetIds` — daftar ID aset yang sedang terikat handover berstatus pending. Dipakai FE (halaman Asset Holder) untuk menonaktifkan tombol Assign tanpa harus memanggil `GET /handover` (endpoint list bertabel/berpaginasi, tidak cocok dipakai hanya untuk cek keanggotaan). Membungkus method service `HandoverService.getPendingItemAssetIds()` yang sebelumnya hanya dipakai internal untuk validasi create. Test & swagger diperbarui.
- **Modul Asset Schedule (kalender jadwal aset)**: modul baru `asset-schedule` (tabel `asset_schedules`) — satu jadwal menautkan `title` + `description` + lampiran (`attachmentIds`, entity `"AssetSchedule"`) ke **satu atau banyak aset** (via tabel junction `asset_schedule_assets`, dikirim sebagai `assetIds`) pada `startDate` (tanpa jam, hanya tanggal), dan bisa **berulang** dengan pola terstruktur: `recurrence` = `none`/`weekly`/`monthly`/`yearly` — **weekly** memilih hari-hari dalam seminggu (`daysOfWeek`, 0=Minggu…6=Sabtu, bisa banyak), **monthly** memilih tanggal (`dayOfMonth`), **yearly** memilih bulan + tanggal (`month` + `dayOfMonth`) — dengan `recurrenceEndDate` opsional sebagai batas seri. Endpoint CRUD `GET/POST /asset-schedule`, `GET/PUT/DELETE /asset-schedule/:id` (permission `asset-schedule:read|create|update|delete`, di-seed) plus **`GET /asset-schedule/calendar?from=&to=`** yang meng-*expand* jadwal berulang jadi daftar occurrence berdasarkan tanggal konkret di rentang window (dipakai FE untuk render kalender). Ekspansi recurrence murni (`recurrence.ts`) melewati tanggal yang tak valid (mis. tanggal 31 di bulan 30 hari, 29 Feb di tahun non-kabisat), mengikuti perilaku kalender standar. Enum `SCHEDULE_RECURRENCES` di `core/enums.ts`. Delete jadwal ikut menghapus lampirannya; list & calendar bisa difilter per `assetId`. Test (`test/asset-schedule.test.ts`, termasuk unit ekspansi recurrence per mode) + factory `createAssetScheduleData` + swagger diperbarui.
- **`inventoryCount` pada `GET /category`, `/sub-category`, `/branch`**: tiap baris kini menyertakan jumlah item Inventory terkait, berdampingan dengan `assetCount` yang sudah ada — Category/SubCategory dihitung dari `inventories.sub_category_id` (via `sub_categories.category_id` untuk Category), Branch dihitung dari jumlah item Inventory yang punya varian dengan saldo stok (`inventory_stock_balances.quantity > 0`) di cabang tsb. Bisa dipakai sebagai `sortBy`. Test & swagger diperbarui — termasuk test baru `test/category.test.ts` & `test/sub-category.test.ts` (sebelumnya kedua modul ini belum punya test file sendiri).
- **Assign user (opsional, multi) pada Asset Schedule + job reminder harian**: `POST/PUT /asset-schedule` kini menerima `userIds` — pengguna yang akan diingatkan soal jadwal tsb, bersifat **opsional** (boleh kosong/tidak diisi = tidak di-assign ke siapa pun) dan **multi** (banyak pengguna), lewat tabel junction baru `asset_schedule_users` (pola sama seperti `asset_schedule_assets`: `setUsers()` di repository men-diff bukan hapus-lalu-insert-ulang). Pada update, `userIds` yang **tidak dikirim** artinya assignment tidak diubah, sedangkan array kosong `[]` secara eksplisit menghapus semua assignment. `AssetScheduleResponse` menyertakan array `users` (id/name/email/photo). Ditambah **`GET /user/options`** — pencarian user ringan untuk picker (pola sama seperti `GET /asset/options`: `q` ke nama/email, `limit` default 20 maks 50, hanya user aktif, tanpa join relasi), sengaja **tidak** mensyaratkan permission `user:read` agar user dengan `asset-schedule:create` saja tetap bisa memilih assignee. Ditambah **job harian** `src/jobs/asset-schedule-reminder.job.ts` (`bun run job:asset-schedule-reminder`, dijadwalkan lewat cron/scheduler eksternal — tidak ada scheduler in-process) yang mengecek jadwal (termasuk hasil ekspansi recurrence) untuk hari ini via `AssetScheduleService.getCalendar()`, lalu mengirim email reminder (template `public/templates/asset-schedule-reminder.html`) ke user yang di-assign **dan** pembuat jadwal (dedup per email). Test & swagger diperbarui.
- **Gambar item pada respons Asset Holder & Stock Out**: `AssetHolderResponse.asset.image` dan `InventoryStockOutResponse.items[].variant.image` (fallback ke gambar item induk jika varian tidak punya gambar sendiri) serta `InventoryStockEntryTemplateResponse.image` (varian, fallback ke gambar item) — dipakai FE untuk menampilkan gambar asli (bukan ikon generik) pada pemilih aset/stok yang dipegang karyawan di form serah terima. Swagger diperbarui.
- **Lampiran pendukung pada serah terima**: `POST /handover` kini menerima `attachmentIds` — dokumen pendukung yang diunggah user, tersimpan berdampingan dengan form tanda tangan yang di-generate otomatis. Saat webhook e-sign menyelesaikan penandatanganan, **hanya form generate-an** (dikenali dari `originalName` = `handover-{id}.pdf`) yang di-repoint ke URL dokumen bertanda tangan; sebelumnya loop-nya menimpa `filename` **semua** lampiran handover, sehingga lampiran user akan ikut tertimpa URL PDF signed begitu fitur ini ada. Test (termasuk kasus anti-timpa) & swagger diperbarui.
- **Hyperlink kolom Code pada export Excel Asset & Inventory**: sel Code (`GET /asset/export`, `GET /inventory/export`) kini berupa hyperlink ke halaman detail item di FE (`${APP_URL}/asset/:id` / `${APP_URL}/inventory/:id`), diberi gaya biru+underline seperti hyperlink gambar yang sudah ada — klik langsung membuka detail aset/item tanpa perlu cari manual. Pada Inventory (rowspan per item), link diset sekali di baris master blok, konsisten dengan pola hyperlink gambar. Test baru di `test/asset.test.ts` & `test/inventory.test.ts`.

### Changed
- **Aset yang ditetapkan lewat serah terima kini boleh juga dikembalikan manual**: `POST /asset-holder/:id/return` tidak lagi menolak holder yang `assignHandoverId`-nya terisi — kebijakan lama ("hanya bisa dikembalikan lewat return handover") dihapus. Return via handover untuk aset yang ditetapkan manual sudah bekerja sejak awal (tidak ada perubahan di sisi itu); untuk stok, `returnStock` juga tidak pernah membatasi berdasarkan asal `assignHandoverId`, jadi sudah simetris tanpa perubahan. Opsi `enforceHandoverPolicy` di `AssetHolderService.returnAsset` dihapus. Test diperbarui.
- **Serah terima tidak lagi memisahkan aset & persediaan (`itemKind` dihapus)**: satu dokumen handover kini bisa membawa `items` (aset) dan/atau `stockItems` (persediaan) sekaligus, bukan harus salah satu — pengembalian tetap hanya boleh atas aset/stok yang benar-benar dipegang oleh "Diserahkan Oleh", sama seperti sebelumnya untuk masing-masing jenis. `Handover.itemKind` (kolom `item_kind`) dihapus dari entity, validator (`CreateHandoverValidator` kini hanya mensyaratkan minimal satu dari `items`/`stockItems`, bukan tepat satu jenis), service (`create`/`approve` digabung memproses kedua jenis dalam satu alur — `approve` hanya menandai `status: "approve"` setelah kedua sisi yang ada berhasil, agar kegagalan sebagian tidak meninggalkan handover berstatus salah), dan serializer. PDF (`generateHandoverPdf`) kini merender satu tabel gabungan (bukan cabang `isStock` terpisah) dengan kolom **Jumlah** yang genuinely terpisah dari **Code** (sebelumnya jumlah stok berbagi slot kolom Code). Test & swagger diperbarui.
- **`GET /inventory` — `balanceCount` dipecah jadi `newCount`/`usedCount`**: daftar item Inventory sekarang mengembalikan total stok on-hand per kondisi (`newCount`, `usedCount`) alih-alih satu angka gabungan `balanceCount`, agar FE bisa menampilkan kolom New/Used terpisah tanpa query tambahan per baris. `sortBy` mendukung `newCount`/`usedCount` (menggantikan `balanceCount`). Swagger (`InventoryResponse` + enum `sortBy`) & `test/inventory.test.ts` diperbarui.
- **Tipe Stock Out jadi boolean `isEmployee`**: kolom `inventory_stock_out.type` (`employee|other`) diganti `is_employee` (boolean); enum `STOCK_OUT_TYPES`/`StockOutType` dihapus dari `core/enums.ts`. `POST /inventory-stock-out` kini menerima `isEmployee` (bukan `type`) — `employeeId` wajib bila `true`, harus kosong bila `false`. Handover assign mengirim `isEmployee: true`. Data lama di dev (`type='employee'` → `is_employee=1`) dimigrasikan manual sebelum sync menghapus kolom `type`. Test & swagger diperbarui.
- **Stock Out kini dokumen + item, seperti Stock In**: `assign` (dulu satu baris `InventoryStockOut` independen per variant/kondisi) sekarang membuat **satu dokumen** `InventoryStockOut` (header: `type`, `employeeId`, `assignedDate`, `assignNote`, `assignHandoverId`, `createdBy`, `attachments`) beserta **baris item** `InventoryStockOutItem` (variant · branch · condition · quantity, plus status kembali per baris: `quantityReturned`, `returnedDate`, `returnNote`, `returnHandoverId`) — persis pola `InventoryStockIn`/`InventoryStockInItem`. `POST /inventory-stock-out` kini mengembalikan **satu objek dokumen** (bukan array), `GET /inventory-stock-out` mengembalikan daftar dokumen dengan `items[]` bersarang. Lampiran (`attachmentIds`) kini cukup diasosiasikan sekali ke dokumen induknya — method `AttachmentService.duplicate` (workaround sebelumnya untuk multi-baris tanpa induk) sudah tidak diperlukan dan dihapus. `returnStock` FIFO kini beroperasi lintas item (join ke dokumen induk untuk `employeeId`), bukan lintas baris top-level. Handover assign yang membawa banyak item kini otomatis menghasilkan **satu dokumen**, bukan N baris terpisah. Test & swagger diperbarui.
- **`inventory-stock-out` jadi modul tersendiri**: dipisah dari `inventory-stock` agar konsisten dengan pola Asset (`asset-holder`, `asset-status`, dll. — satu dokumen/tabel = satu modul top-level), sama seperti pemisahan `inventory-stock-transfer`/`inventory-stock-in` sebelumnya. Entity `InventoryStockOut`, validator assign/return, service (assign/returnStock/assertCanAssign/assertCanReturn/getRemainingHeld), controller, dan serializer pindah ke `src/modules/inventory-stock-out/`; `InventoryStockService` (modul lama) kini hanya menangani balance/entry, dengan `increaseBalance`/`decreaseBalance`/`getAvailable` dipakai lintas-modul lewat DI. Route pindah dari `/inventory/stock/assign|return|out` ke **`POST/GET /inventory-stock-out`** & **`POST /inventory-stock-out/return`** (permission tetap sama: `inventory-stock:read|assign|return`). Handover kini bergantung pada `InventoryStockOutService`, bukan `InventoryStockService`. Test assign/return dipindah ke `test/inventory-stock-out.test.ts` (mengikuti pola `inventory-stock-transfer.test.ts`).
- **Assign stok jadi "Stock Out" bertipe (employee/other)**: `inventory_stock_holdings` → **`inventory_stock_out`** (entity `InventoryStockHolding` → `InventoryStockOut`), tambah kolom `type` (`employee|other`, enum `STOCK_OUT_TYPES`). `employeeId` kini nullable — `type: "other"` (dipakai/dibuang/dijual, dll., tanpa karyawan tujuan) membuat baris yang langsung "resolved" (`quantityReturned = quantity`, `returnedDate` terisi saat dibuat) sehingga otomatis tidak bisa di-return, tanpa perlu logic tambahan di alur return/UI. `POST /inventory/stock/assign` menerima `type` wajib (`employeeId` wajib hanya bila `type: "employee"`, divalidasi via `superRefine`). `GET /inventory/stock/holding` → **`GET /inventory/stock/out`**; filter `active` kini `(quantity > quantityReturned OR type = 'other')` agar baris `other` selalu tampil (satu-arah, tak pernah basi). Handover assign tetap selalu `type: "employee"`. Swagger & `test/inventory.test.ts` diperbarui (kasus baru untuk `type: "other"` & validasi `employeeId`).
- **Refactor sisa penamaan `product` → `inventory`/`item`**: variabel lokal & parameter di modul inventory (`inventory.service.ts`, `inventory.controller.ts`, `inventory.serialize.ts`, `countVariants(productId)` → `countVariants(inventoryId)` di interface), `productName` → `inventoryName` pada PDF handover, serta redaksi `swagger.yaml` ("stock product" → "inventory item", "Product not found" → "Inventory item not found", dll.) dan komentar/variabel test. Tidak ada perubahan endpoint/kolom.

### Added
- **`GET /inventory/export` — export Excel lengkap Item → Varian → Cabang → Kondisi, hirarki di-rowspan**: satu baris fisik per leaf (item, varian, cabang × kondisi) — item tanpa varian atau varian tanpa stok tetap dapat satu baris dengan kolom yang lebih dalam dikosongkan. Kolom level Item (No, Image, Code, Name, Description, Category, Sub Category, Unit, Status, label dinamis) dan level Varian (Variant Name, Variant Code, Variant Description) di-**merge cell (rowspan)** membentang seluruh baris leaf miliknya — bukan diulang tiap baris — supaya hirarkinya kebaca visual di spreadsheet; baris selang-seling & border ikut per-blok item. Kolom Branch/Condition/Quantity tetap satu nilai per baris (level terdalam). Mendukung filter & search yang sama seperti `GET /inventory`. Mengikuti pola kode `AssetUtilService.export` untuk sisanya (header 2-baris berkelompok, autofilter, hyperlink gambar) — `InventoryUtilService` baru, permission `inventory:export` baru (di-seed). Swagger & `test/inventory.test.ts` (4 test baru, termasuk parsing buffer xlsx untuk verifikasi baris) diperbarui.
- **Advance filter pada `GET /inventory`**: menyertakan `categoryIds`, `subCategoryIds`, `units`, `isActive`, `variantStatus` (`has_variants`/`no_variants`), `newStockMin`/`newStockMax`, `usedStockMin`/`usedStockMax` (memakai subquery `newCount`/`usedCount` yang sama seperti kolom list), `missingFields` (`image`/`description`/`subCategory`), dan `label.{key}=value` — pola query param & builder-nya sama seperti `GET /asset`. Swagger & `test/inventory.test.ts` (7 test baru) diperbarui.
- **Gambar & deskripsi pada varian**: kolom baru `image` + `description` pada `inventory_variants`. Endpoint `POST/PUT /inventory-variant` menerimanya; serializer varian kini async mengembalikan `image` (URL ter-resolve) + `description`. Inline-variant pada `POST /inventory` juga menerima `image`/`description`. Swagger (`InventoryVariantResponse` + request create/update, serta variants pada `CreateInventoryRequest`) & `test/inventory.test.ts` diperbarui.
- **Auto-isi `code` dari id bila kosong (seperti category)**: saat create item Inventory & varian (termasuk inline-variant), bila `code` kosong maka diisi `String(id)` setelah tersimpan. Kode eksplisit tetap dipertahankan. Test disertakan.
- **Hitungan pada daftar item Inventory**: `GET /inventory` kini menyertakan `variantCount` (jumlah varian) & `balanceCount` (total stok on-hand semua varian/cabang) per item, dan mendukung `sortBy` untuk `category`, `subCategory`, `unit`, `variantCount`, `balanceCount` (selain `name`, `code`, `createdAt`). Query di-refactor 2-langkah agar pagination/sort tetap benar dengan relasi label (one-to-many). Swagger (`InventoryResponse` + enum `sortBy`) & `test/inventory.test.ts` diperbarui.
- **Riwayat transfer stok (dokumen transfer)**: transfer mencatat dokumen tersendiri — tabel `inventory_stock_transfers` (cabang asal/tujuan, catatan, pembuat) + `inventory_stock_transfer_items` (varian · kondisi · qty), dengan `attachmentIds` (ditautkan ke entity `"InventoryStockTransfer"`). `GET/POST /inventory-stock-transfer` (permission `inventory-stock:read|transfer`) mengembalikan/menerima riwayat transfer per item beserta item, pembuat, dan lampiran.
- **Lampiran & pembuat pada item Inventory**: `POST/PUT /inventory` menerima `attachmentIds` (ditautkan ke entity `"Inventory"`); response `InventoryResponse` mengembalikan `attachments` + `createdBy`. Swagger + test disesuaikan.
- **Tambah Stok (stock-in, dokumen)**: `POST /inventory-stock-in` (permission `inventory-stock:entry`) — **menambah** (increment) jumlah Baru/Bekas ke saldo, **multi-cabang × multi-varian** dalam satu request (`items: [{ branchId, variantId, new, used }]`), mencatat dokumen tersendiri — tabel `inventory_stock_in` (catatan, pembuat) + `inventory_stock_in_items` (varian · cabang · kondisi · qty · `balanceAfter`) — dengan **lampiran** (`attachmentIds`). `GET /inventory-stock-in?inventoryId=` mengembalikan riwayat stock-in per item, seperti transfer. Beda dengan `entry` yang bersifat opname (set absolut).
- **Item Inventory diperkaya seperti Asset**: kolom baru pada `inventories` — `image`, `unit` (satuan), `sub_category_id` (FK ke `SubCategory`, kategori diturunkan) — plus tabel **`inventory_labels`** untuk custom label. Endpoint `POST /inventory` kini **atomik**: sekaligus membuat item + label + varian + **stok awal per varian × cabang × kondisi** (satu transaksi). Tambah `GET /inventory/label-keys` (untuk kolom label dinamis). Swagger + `test/inventory.test.ts` diperbarui (create kaya + label-keys).
- **Satuan dipindah ke level item**: kolom `unit` dihapus dari `inventory_variants`; semua saldo/tampilan stok memakai satuan item (`variant.inventory.unit`).
- **Modul Inventory (persediaan)**: fitur baru bergaya seperti Asset — satu menu `Inventory`/`Persediaan`, daftar item master (`inventory`) dengan halaman detail bertab (Stok, Stock In, Transfer, Assign/Return). Struktur mengikuti pola Asset (satu modul child = satu tabel top-level, bukan nested): `inventory` (item master, tabel `inventories`), `inventory-variant` (varian, `inventory_variants`), `inventory-stock` (saldo & holding, `inventory_stock_balances|holdings` — juga tempat `entry`/`assign`/`return`), `inventory-stock-transfer` (dokumen transfer, `inventory_stock_transfers|transfer_items`), `inventory-stock-in` (dokumen stock-in, `inventory_stock_in|in_items`), `inventory-log` (audit trail, `inventory_logs`). Monitoring saldo per **cabang × varian × kondisi (baru/bekas)**, stock entry (opname), assign/return ke karyawan. Endpoint `/inventory*`, `/inventory-variant*`, `/inventory/stock*`, `/inventory-stock-transfer*`, `/inventory-stock-in*`, `/inventory-log`; permission `inventory:*`, `inventory-variant:*`, `inventory-stock:*`.
- **Assign & Return stok ke karyawan**: `inventory-stock:assign|return` — assign mengurangi saldo cabang (tak boleh negatif) dan membuat `inventory_stock_holdings`; return selalu masuk ke kondisi **`used`**, tidak boleh melebihi yang masih dipegang karyawan (konsumsi FIFO). Bisa **manual** (`POST /inventory/stock/assign|return`) maupun **lewat handover**.
- **Modul `inventory-log`** (audit trail, seperti `asset-log`): satu baris per aksi pada item Inventory — `create`/`update` pada item itu sendiri (module `inventory`), serta `entry`/`stock_in`/`transfer`/`assign`/`return` pada stoknya (module `stock`). Endpoint `GET /inventory-log?inventoryId=` (permission `inventory:read`). Delete item **tidak** di-log (FK `inventory_id` cascade — baris log akan ikut terhapus, sama seperti `AssetService.delete`).
- **`inventory-stock-transfer` & `inventory-stock-in` jadi modul tersendiri**: dipisah dari `inventory-stock` agar konsisten dengan pola Asset (`asset-holder`, `asset-status`, dll. — satu tabel/dokumen = satu modul top-level). Route pindah dari `/inventory/stock/transfer` & `/inventory/stock/add` ke `/inventory-stock-transfer` & `/inventory-stock-in` (permission tetap sama: `inventory-stock:read|transfer|entry`). `InventoryStockService` menambah method publik `increaseBalance`/`decreaseBalance` agar modul lain bisa memutasi saldo lewat DI, bukan akses repository langsung.
- **Hapus ledger pergerakan stok (`inventory_stock_movements`)**: tabel & entity `InventoryStockMovement`, endpoint `GET /inventory/stock/movement`, dan tipe `StockMovementType` dihapus — riwayat kini sepenuhnya lewat dokumen (Stock In, Transfer, Assign/Return) dan `inventory-log`, tanpa ledger flat terpisah.
- **Handover stok**: `Handover` diperluas polimorfik (`item_kind` = `asset|stock`) dengan tabel item terpisah `handover_stock_items`; memakai alur `pending → e-sign → approve` yang sama, dan PDF merender baris stok (produk · varian · kondisi · qty · cabang). Test E2E `test/inventory.test.ts` & `test/handover-stock.test.ts`; swagger diperbarui.
- **Custom field handover (per transaction type)**: modul baru `handover-field` — field konfigurabel per tipe (`assign`/`return`) dengan tipe `text | number | select | radio | date | datetime`, opsi custom untuk select/radio, dan flag required. Endpoint `GET /handover-field?transactionType=` & `PUT /handover-field/:transactionType` (bulk-replace, permission `handover-field:read|manage`). Saat handover dibuat, nilai field di-**snapshot** ke kolom `handovers.custom_fields` (`simple-json`) → mengedit definisi field **tidak** memengaruhi handover yang sudah ada. Key & value tampil di PDF. Test E2E `test/handover-field.test.ts` + skenario snapshot/versioning di `test/handover.test.ts`.
- **Serah terima pengembalian (return handover)**: handover `return` — hanya aset yang active holder-nya = karyawan penyerah (`handedOverById`) yang boleh dipindai; lewat alur `pending → e-sign → approve` yang sama; saat approve, holder terkait ditandai returned. Mendukung **partial return** (assign 2, return 1) dan tautan best-effort ke handover assign asal (`parent_handover_id`). Holder dari serah terima hanya bisa dikembalikan lewat return handover (return manual diblokir).
- **Guard perubahan status aset**: menolak ubah status (single & bulk) bila aset masih **dipegang via handover** atau sedang dalam **pending handover** — menjaga konsistensi lifecycle handover. Bulk menolak seluruh batch bila ada satu yang terkunci.
- Test E2E return handover + guard status (`test/handover.test.ts`).

### Fixed
- **`swagger.yaml` gagal di-parse (`bad indentation of a mapping entry` di baris 4667)**: deskripsi `POST /inventory-stock-out` berupa plain scalar (tanpa quote) yang berisi `` `isEmployee: true` `` — kombinasi titik dua + spasi di dalam scalar YAML tanpa quote ambigu dengan awal mapping baru. Deskripsinya dibungkus tanda kutip ganda.
- **`getMyBorrowedBooks` filter buku aktif di level query, bukan in-memory**: sebelumnya mengambil 100 baris via `assetHolderService.getAll` lalu memfilter `!returnedDate` di JS (bisa salah/kepotong bila peminjaman aktif > 100). Kini `BookRepository.findActiveLoans(employeeId)` query langsung `category = Buku AND employeeId AND returnedDate IS NULL`.

### Changed
- **Refactor relasi/key `product` → `inventory`** (mengikuti entity `Inventory`): `inventory_variants.product_id` → **`inventory_id`**, property `productId` → `inventoryId`, relasi `product` → `inventory`; method `getByProduct`/`findByProduct` → `getByInventory`/`findByInventory`; query param & payload `productId` → `inventoryId` (variant, stock entry, filter monitoring); serializer/PDF/handover membaca `variant.inventory`. Swagger + test ikut disesuaikan.
- **Rename modul `AssetHandover` → `Handover`** (generik, untuk reuse modul stok — stok & serah terima berbagi satu dokumen): folder `src/modules/handover`, tabel `handovers` & `handover_items`, endpoint `/handover*`, permission `handover:*`, entity-type attachment `"Handover"`.
- **Cleanup serializer (repo-wide)**: hapus scalar FK bila objek lengkap sudah diserialisasi (mis. `assetId` bila ada `asset:{}`), pada holder/asset/user/user-list/note/location/maintenance. `UserService.create/update` kini reload relasi agar objek `role`/`employee` ada di response.
- Entity `AssetHolder`: relasi origin handover `handover`/`handover_id` → **`assignHandover`/`assign_handover_id`**; tambah relasi `returnHandover`/`return_handover_id` (diisi saat return handover di-approve).
- Copy PDF & dokumen: type transaksi Penetapan/Pengembalian.

### Added
- **Cancel Asset Handover**: handover berstatus `pending` dapat dibatalkan (status menjadi `cancel`); setelah `approve` handover terkunci dan tidak bisa dibatalkan. Endpoint `POST /asset-handover/:id/cancel` (permission `asset-handover:cancel`); aset ikut terbebas untuk di-assign ulang. Alur e-sign tetap aman: webhook `COMPLETED` menolak meng-approve handover yang sudah `cancel`.
- **Module Asset Handover** (Form Serah Terima Aset/Barang): dokumen serah terima berisi header (tanggal, lokasi teks bebas, type, catatan) + daftar item aset (asset, employee penerima, jumlah, kelengkapan, kondisi).
  - Type transaksi: `assign | return`. Status alur: `pending | approve | reject | cancel`.
  - Satu handover dapat memiliki banyak employee (employee dipasang per item; satu employee bisa memegang banyak aset).
  - Hanya aset tanpa active holder & tidak sedang di handover `pending` lain yang boleh dipilih.
  - Saat `approve` → otomatis membuat asset holder tiap item (asset → employee). Cancel hanya untuk handover `pending`; setelah `approve` handover terkunci.
  - Endpoint: `GET/POST /asset-handover`, `GET /asset-handover/:id`, `POST /asset-handover/:id/cancel` (permission `asset-handover:*`). Approve/reject dipicu via webhook e-sign.
- **E-sign integration untuk Asset Handover**:
  - Saat handover dibuat → otomatis men-generate PDF "FORM SERAH TERIMA ASET/BARANG" (tanggal, nama karyawan, checklist type: Penetapan/Pengembalian, catatan, tabel item berisi nama/code/deskripsi, kolom tanda tangan penyerah & penerima), menyimpannya sebagai attachment handover, dan mengirimkannya ke provider e-sign (`esignHelper.documentSign`) dengan signer = penyerah + penerima.
  - Webhook `POST /webhook/esign` status `COMPLETED` → handover di-approve; `filename` attachment handover diganti ke `file_url` dokumen tertanda, dan dokumen tertanda tersebut juga di-attach ke setiap asset holder yang dibuat.
  - Helper baru: `src/core/helpers/handover-pdf.ts` (`generateHandoverPdf`) memakai `pdf-lib`.
  - `AttachmentService`: tambah `createExternal()` (attachment dari URL eksternal) & `updateFilename()`.
- Entity baru: `asset_handovers`, `asset_handover_items`.
- Permission baru: `asset-handover` × `read, create, update, delete, approve, reject, cancel`.
- Test E2E: `test/asset-handover.test.ts` (23 test — termasuk generate attachment form saat create, approve/reject via webhook e-sign, attachment tertanda ke holder, & skenario cancel).

### Changed
- Entity `AssetHolder`: tambah kolom nullable `handover_id` (diisi saat handover di-approve) + relasi ke `AssetHandover`.
- `AssetHolderService.create`: memblokir assign holder manual bila aset sedang menunggu approval handover (`pending`).
- Handover: field `purpose` (Keperluan) diganti menjadi `note` (Catatan) — kolom DB `note`, ditampilkan di form, detail, dan PDF form serah terima.
- Handover: `transactionType` disederhanakan dari `serah_terima | peminjaman | pengembalian` menjadi `assign | return` (label form "Type"; label PDF Penetapan/Pengembalian).
- **Sentralisasi enum**: semua enum bernilai tetap dipindah ke `src/core/enums.ts` (`ASSET_STATUSES`, `HANDOVER_TRANSACTION_TYPES`, `HANDOVER_STATUSES`, `ASSET_LOCATION_SOURCES`) — validator (Zod) & entity (TypeORM) memakai sumber yang sama, jadi perubahan nilai cukup di satu tempat.

### Removed
- Handover: field `category` (Status Aset) & `estimatedReturnDate` (Estimasi Kembali) dihapus total — dari form, detail, list, validator, kolom DB, swagger, dan PDF (checklist "Status Asset" & baris "Estimasi Tanggal Kembali").

### Fixed
- Handover: catatan (`note`) sebelumnya tidak tersimpan karena frontend mengirim `note` sementara backend memvalidasi `purpose`; kini konsisten memakai `note` end-to-end.

---

## [0.1.0] — 2026-06-18

### Added
- Initial release

---

## Template Entri Baru

```markdown
## [X.Y.Z] — YYYY-MM-DD

### Added
- Fitur baru

### Changed
- Perubahan pada fitur yang sudah ada

### Deprecated
- Fitur yang akan dihapus di versi mendatang

### Removed
- Fitur yang dihapus

### Fixed
- Perbaikan bug

### Security
- Perbaikan keamanan
```

### Versioning Rules

- **MAJOR** (X.0.0): Breaking changes, perubahan arsitektur besar
- **MINOR** (0.X.0): Fitur baru, module baru, penambahan endpoint
- **PATCH** (0.0.X): Bug fix, perbaikan kecil, update dependencies
